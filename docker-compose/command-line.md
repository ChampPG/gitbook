# Command line

### Command Line Examples

Enter the command line using [run](https://docs.docker.com/engine/reference/commandline/compose\_run/)

```
docker-compose run {instance_name} bash
```

Enter command line using [exec](https://docs.docker.com/engine/reference/commandline/compose\_exec/)

```
docker-compose exec {instance_name} /bin/bash
```
